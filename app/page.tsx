const Home = async () => {
    return (
        <section className="grid grid-cols-[repeat(auto-fit,minmax(300px,1fr))] gap-10">
            AAAAAAAAAAAA
        </section>
    )
}

export default Home
