export const metadata = {
    title: `Error • Megan's Page`,
    robots: 'noindex, nofollow',
}

const AuthErrorLayout = ({ children }: { children: React.ReactNode }) => {
    return <>{children}</>
}

export default AuthErrorLayout
